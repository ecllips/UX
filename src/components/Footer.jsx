import styles from './Footer.module.css'; // Подключение CSS

const Footer = () => {
    return (
        <footer className={styles.footer}>
            UX/UI Alina Stankovych 2024
        </footer>
    );
};

export default Footer;
